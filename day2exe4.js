let num1 = 25;
let num2 = 40;

if (num1 > num2) {
    console.log("\nLargest Number:", num1);
} else {
    console.log("\nLargest Number:", num2);
}

let a = 15;
let b = 30;
let c = 20;

if (a >= b && a >= c) {
    console.log("Largest among three:", a);
} else if (b >= a && b >= c) {
    console.log("Largest among three:", b);
} else {
    console.log("Largest among three:", c);
}
